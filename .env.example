# OpenSky Network — OAuth2 client_credentials.
# Create at: https://opensky-network.org/my-opensky/account → "Create API client".
# Without creds: 400 credits/day (~unusable for a live Europe map).
# Authenticated: 4000 credits/day (≈ 1 Europe call per ~90s).
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
